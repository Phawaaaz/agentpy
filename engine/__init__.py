"""Engine — the agent loop and everything it acts through: the orchestrator,
the permission chokepoint, the tool registry, MCP connectivity, and the
built-in tools (engine/builtin/). See DESIGN.md D20.
"""
