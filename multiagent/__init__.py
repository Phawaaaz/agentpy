"""Multi-agent coordination: a `delegate` tool the coordinator's agent loop
can call to hand a self-contained sub-task to a role-specific sub-agent.
See DESIGN.md D17 for why this is a deliberate, documented extension of D1
("not a multi-agent framework") rather than a change to it.
"""
