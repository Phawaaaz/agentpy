# The harness stays model-independent via native SDKs behind one Provider
# interface. The OpenAI provider also accepts a base_url, so it drives ANY
# OpenAI-compatible endpoint (Ollama, LM Studio, OpenRouter, Groq, Together, vLLM).
anthropic>=0.40.0
openai>=1.40.0

# Loads API keys / settings from a .env file so they stay out of the code.
python-dotenv>=1.0.0

# MCP client: connect to external MCP servers (engine/mcp_client.py).
mcp>=1.2.0

# Optional .harness.yaml config file support (config.py falls back to env-only
# without it, but the YAML feature silently no-ops if this is missing).
pyyaml>=6.0

# Relational store for users/sessions/usage (storage/, D29). SQLite by
# default via HARNESS_DB_URL; the same code drives Postgres by URL swap.
sqlalchemy>=2.0
